{
  "name": "myTemplate",
  "generator": {
    "renderer": "react",
    "parameters": {
      "package": {
        "description": "Package name",
        "required": false,
        "default": "com.asyncapi"
      }
    }
  },
  "dependencies": {
    "@asyncapi/generator-react-sdk": "^0.2.25"
  }
}